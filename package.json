{
  "name": "card-news-generator",
  "version": "1.0.0",
  "description": "AI 기반 카드뉴스 자동 생성기 - Cloudflare Pages",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev . --port 8788",
    "deploy": "npx wrangler pages deploy .",
    "preview": "npx wrangler pages dev . --port 8788 --live-reload"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
